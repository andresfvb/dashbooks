module.exports = {
    images: {
      unoptimized: true,
    },
    distDir: 'out',
    target: 'experimental-serverless-trace',
  // Exporta tu aplicación como HTML estático
  output: 'export',
  };